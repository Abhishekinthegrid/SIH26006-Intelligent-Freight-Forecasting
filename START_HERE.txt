SIH26006 FINAL OPTIMIZED - START HERE

1) Backend terminal
cd "C:\SIH project v4\backend"
.\venv\Scripts\python.exe -m pip install -r requirements.txt
.\venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000

2) Frontend terminal
cd "C:\SIH project v4\frontend"
npm.cmd install
npm.cmd run dev

3) Open
http://localhost:5173

Do NOT run RETRAIN_MODEL.bat just to start the website. The saved model is loaded at API startup.

If /api/route returns 503 mentioning SeaRoute, run the pip install command above in THIS backend venv.
